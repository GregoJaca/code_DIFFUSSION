#!/bin/bash

# Delete all files and subdirectories within the 'inputs' directory
rm -rf inputs/*

# Delete all files and subdirectories within the 'outputs' directory
rm -rf outputs/*

echo "Cleaned 'inputs' and 'outputs' directories."
